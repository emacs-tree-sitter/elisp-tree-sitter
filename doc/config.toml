# XXX: Hugo, or its themes/plugins don't work well with relativeURLs
baseURL = "https://ubolonton.github.io/emacs-tree-sitter"
# relativeURLs = true

languageCode = "en-us"
title = "Emacs Tree-sitter"

googleAnalytics = "UA-138609797-2"

theme = [
  "docdock",
  # "learn",
  # "alabaster",
  # "hugo-bare-min-theme",
  # "techdoc",

  # "docsy",
  # "book",
  # "docuapi",
]

[markup.highlight]
# style = "github"
noClasses = false

[params]
themeStyle = "original"

[outputs]
home = [ "HTML", "RSS", "JSON"]
